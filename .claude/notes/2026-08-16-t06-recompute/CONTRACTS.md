# CONTRACTS — wave `2026-08-16-t06-recompute`

**Frozen by the orchestrator before the first agent started. No agent may change anything in this
file.** If you believe a contract is wrong, **`SendMessage` the orchestrator (`main`) and keep
working on a recorded assumption** — do not renegotiate with a peer and do not edit this file.

`BASE_SHA = 8afa59e5fd926cc1bb9c841800bfd870f042b8a4` (branch `ticket/T-06`)

---

## 0. The wave

| Track | Branch | Worktree | Owns |
|---|---|---|---|
| **encoding** | `t06/encoding` | `.../IsalGraph-T06/.claude/worktrees/encoding` | `benchmarks/real_data/eval_encoding/t06_*.py`, `tests/unit/test_t06_encoding.py` |
| **distance** | `t06/distance` | `.../IsalGraph-T06/.claude/worktrees/distance` | `benchmarks/real_data/eval_distance/**`, `tests/unit/test_t06_distance.py` |
| **stats** | `t06/stats` | `.../IsalGraph-T06/.claude/worktrees/stats` | `benchmarks/real_data/eval_stats/**`, `tests/unit/test_t06_stats.py` |

Ownership sets are **disjoint**. Everything not listed for your track is **read-only**.

**Data flow: encoding → distance → stats.** You do **not** wait for your upstream. You code against
the schemas in §2–§4 and test with synthetic fixtures you build yourself.

---

## 1. 🔴 Prohibitions — read these before writing any code

1. **Never edit `src/isalgraph/`.** In particular `src/isalgraph/competitors/` is being edited *right
   now* by another session (ticket T-04a). It is read-only to you.
2. **`import isalgraph` does NOT resolve to your worktree.** The `scikit-build-core` editable finder
   is path-pinned to `/home/mpascual/research/code/IsalGraph/src/isalgraph/`, so it outranks your
   worktree and `PYTHONPATH`. **Verified 2026-08-16.** You are importing another branch's live code.
   Consequences you must respect:
   - **Never assert a numeric value produced by a competitor backend.** Assert *API shape* and
     invariants — `bits.count()` returns both conventions; `distance(a, a) == 0`; a distance is
     symmetric — never "graph6 on this fixture is 42 bits". Those numbers can change under you with
     no error raised.
   - **Never `export PYTHONPATH=<worktree>/src`** to "fix" this. A src-first path shadows the
     installed package and silently falls back to the pure-Python engine, so your benchmark measures
     nothing and nothing errors.
3. **Never run a production campaign.** No full-cohort encode, no full distance matrix, no
   full bootstrap. Validate on ≤ 200 graphs. The orchestrator runs production.
4. **Never report a timing as a result.** Three agents share one workstation; every concurrent
   measurement is contaminated. You may build and exercise a timing harness; you may not publish a
   number from it.
5. **No Picasso.** No `ssh`, no `sbatch`, no `rsync`, no `squeue`. You may *write* SLURM scripts (load
   the `picasso-sbatch` skill first, run `bash -n`, paste a dry run). The orchestrator submits.
6. **Nothing in `scratchpad/`.** That directory is how this project lost thirteen measurement
   scripts. Code goes in `benchmarks/`, tests in `tests/`.
7. **Never edit a plan file** under `.claude/notes/review/plan/`, and never edit `tickets.md`. Report
   corrections to the orchestrator; propagating them is the orchestrator's job.
8. **Never `git switch`, commit, or write in `/home/mpascual/research/code/IsalGraph`** (the shared
   checkout). Work only inside your own worktree.

---

## 2. Shared constants — frozen

```python
SEED = 42                       # everywhere, no exceptions
ENCODE_BUDGET_S = 300.0         # D14, per graph, enforced by a KILLED SUBPROCESS
COMPUTABILITY_THRESHOLD = 0.99  # PER (representation, dataset): computable iff >= 99% of that
                                # dataset's graphs encode within ENCODE_BUDGET_S
```

> **The threshold is per `(representation, dataset)`, never per representation.** It feeds the
> pre-registration's `c` term (`preregistration.md` §5.1, amended 2026-08-16): `agm_cam` completes on
> **100 %** of Letter ×3 and LINUX while failing 24–98 % on GREC / AIDS-IAM / COIL-DEL / Protein /
> Mutagenicity, so an all-or-nothing gate would delete tests we can actually run. **The encoding
> track's job is to emit the per-(representation, dataset) completion rate; it does not decide `c`
> and must not.** The orchestrator applies the rule.
>
> Other tickets' completion rates are **not** this measurement — they use each backend's own budget
> (`agm_cam` → `AGMBudgetExceeded`; `min_dfs` → `max_projections = 50,000`; `isalgraph_pruned` →
> `CanonicalizationTimeoutError` at **2 s**), not 300 s. Measure it here, under this budget.
>
> ### The criterion is "completes under its FROZEN CONFIGURATION within 300 s" — decided 2026-08-16
>
> Raised by `t06-encoding` as assumption A1. **The backends keep their frozen internal caps**
> (`agm_cam`'s `search_nodes`, `min_dfs`'s `max_projections = 50,000`). Two reasons: those caps are
> part of each backend's frozen T-04 specification, so lifting them **moves a published ceiling**;
> and `min_dfs`'s cap is a **memory** guard — the first Suite-2 run was **OOM-killed (exit 137)**
> because the construction holds every embedding realising the current minimal prefix, which at
> `n = 92` grows without bound. Lifting that one risks killing the campaign, not just changing a
> number.
>
> **But the two failure modes are recorded separately.** `error_kind` carries the exception class
> (`AGMBudgetExceeded`, `MinDfsBudgetExceeded`, `CanonicalizationTimeoutError`, `Killed`), so the
> report can state *internal-cap* failures and *wall-clock* failures as distinct columns. A rate that
> conflates them is not interpretable, and separating them costs nothing.
>
> The `--agm-search-nodes` / `--min-dfs-max-projections` overrides stay available for a **labelled
> sensitivity arm** only. They are never the primary reading.

**Dataset keys, in this exact spelling and order** (they are the `.npz` basenames throughout):

```python
SUITE2 = ["iam_letter_low", "iam_letter_med", "iam_letter_high", "linux", "aids_graphedx",
          "grec", "aids_iam", "coil_del", "mutagenicity", "protein"]
SUITE1 = ["iam_letter_low", "iam_letter_med", "iam_letter_high", "linux", "aids"]
```

> ⚠ **Suite 1 and Suite 2 are different cohorts even where the name matches.** Suite 1 applies
> `n_max = 12`; `aids` (Suite 1) has **769** graphs, `aids_graphedx` (Suite 2) has **819**.
> **Always join on `graph_ids`, never positionally.**

**Representations.** Backend names as registered in `isalgraph.competitors`:

| Role | Names |
|---|---|
| **Claim A serialisations (the frozen 6)** | `graph6`, `sparse6`, `nauty_graph6`, `adjacency`, `agm_cam`, `min_dfs` |
| **Claim B comparators (the frozen 7)** | the 6 above **+ `wl_subtree`** |
| **Reference arm** | `isalgraph_pruned` — **provisional, orchestrator confirms via F-1**. Also emit `isalgraph_canonical` wherever it completes |
| **Baseline, never a comparator** | `size_null` |
| Exploratory extra | `sparse6_nauty` — emit it, but it is **not** in the frozen family |

`wl_subtree` and `size_null` raise `BitCountUndefined` — that is **correct behaviour**, record the
reason, do not fabricate a bit count. **WL depth is `h = 2` ≡ `n_iter = 2`.**

**D15 tiers — frozen, never recomputed at run time:**

```python
D15_TIERS = {
    "linux": 1, "protein": 1, "grec": 1, "aids_graphedx": 1,
    "iam_letter_low": 1, "iam_letter_med": 1,
    "aids_iam": 2, "iam_letter_high": 2,
    "coil_del": 3, "mutagenicity": 3,
}
# tier 1: 2000 replicates, 9999 permutations, all pairs
# tier 2: 2000 replicates, 4999 permutations, all pairs
# tier 3: 1000 replicates, 1999 permutations, uniform subsample of 2_000_000 induced pairs, seed 42
```

**Cohort source (read-only):**

```
/media/mpascual/Sandisk2TB/research/ISAL/completed/isalgraph/data/source/APPROX_GED/exported_suite2/{key}.npz
```

CSR format: `n_nodes int32 (G,)`, `n_edges int32 (G,)`, `edge_offsets int64 (G+1,)`,
`edges int32 (2, E)` (node ids **local** `0..n_nodes-1`), `graph_ids`, `splits`, `labels <U1`
(per-graph **class** label, `''` when the dataset has none), `metadata` (0-d JSON string).

> ## ⚠ CORRECTED 2026-08-16 — this paragraph was wrong in two ways. Both were caught by
> `t06-encoding` and **verified by the orchestrator** before the correction was made.
>
> **1. `edges` stores ONE orientation (`u < v`), not both.** The original text said both
> orientations, so `edge_offsets` spans were `2 × n_edges`. **Measured**: `edge_offsets[-1] ==
> sum(n_edges)` exactly, ratio **1.000**, on `iam_letter_low` (3,618), `linux` (743) and `protein`
> (34,957); every pair satisfies `u < v`. **A loader that followed the original contract and halved
> the span would have read half the edges of every graph in the cohort, with no error raised.**
>
> Write loaders that are correct under either layout — de-duplicate on `frozenset({u, v})` and
> **assert the recovered count equals `n_edges`**. That assertion is what makes this class of defect
> loud instead of silent.
>
> **2. The dtypes are NOT uniform across datasets.** Measured: `splits` is `<U10` on
> `iam_letter_low` but `<U5` on `linux` and `protein`; `graph_ids` is `<U8` / `<U16` / `<U10`
> respectively. **And the split vocabulary differs**: `validation` (Letter) vs `val` (LINUX) vs
> `valid` (Protein). Never hard-code a width, and never match a split by name across datasets.
> Splits are merged anyway (decision 3), so read them for provenance only.

**GED matrices (read-only)**, same 10 keys, **square** `float64 (G, G)`:

```
.../APPROX_GED/{LB,UB,UB_SENSITIVITY}/{key}.npz
   ged_matrix, lb_matrix, ub_matrix float64 (G,G) · certified_mask bool (G,G)
   seconds_matrix float32 · node_counts, edge_counts int32 (G,) · graph_ids <U16 · labels <U1
   metadata (0-d JSON)
.../GED_PRECOMPUTED/extended_merged_exact_ged/computed/{suite1_key}.npz   # same keys
```

Censored entries carry **`inf`, never `NaN`** — filter with `np.isfinite`, select on
`certified_mask`. **GED is legitimately 0** for isomorphic graphs; never assert `value > 0` per pair.

---

## 3. Schema — encodings `.npz` (produced by **encoding**, consumed by **distance**)

One file per `(suite, dataset, representation)`:
`encodings/{suite}/{dataset}__{representation}.npz`

| key | dtype | shape | meaning |
|---|---|---|---|
| `graph_ids` | `<U16` | `(G,)` | **in cohort order**, identical to the cohort file's `graph_ids` |
| `node_counts` | `int32` | `(G,)` | carried through from the cohort — **the distance track gets `n` from here and never opens a cohort file**, which is what keeps the two ownership sets disjoint |
| `edge_counts` | `int32` | `(G,)` | carried through from the cohort |
| `encoding` | `<U…` (object-free) | `(G,)` | the encoded **symbol sequence**, joined by `metadata.symbol_sep`; `''` only when `status == "error"` — see §3.2 |
| `length` | `int32` | `(G,)` | **the SYMBOL count** (`Encoding.length`), not `len(encoding)`; `-1` when not encoded |
| `error_kind` | `<U32` | `(G,)` | exception class name when `status == "error"`, else `''` — see §3.1 |

### 3.1 🔴 Symbols are the unit, not characters — corrected 2026-08-16

Raised by `t06-encoding`. **`Encoding.symbols`, not `Encoding.text`, is the comparison unit.** For
every backend except `min_dfs` one symbol is one character and the two coincide. For **`min_dfs` a
symbol is a whole DFS tuple**, so a character-level Levenshtein over the rendered text charges ~4
edits for one deleted tuple — the twofold error `competitors/base.py` warns about, on the comparator
[competitors](../review/plan/competitors.md) §2 calls *"the single most important comparator"*.

**Frozen convention:**

- `metadata.symbol_sep` is a **one-character separator** — **`"\x1f"` (ASCII unit separator)** — for
  any backend whose symbols are not single characters, and **`""`** otherwise.
- `encoding` is `symbol_sep.join(str(s) for s in Encoding.symbols)` when `symbol_sep != ""`, and
  `Encoding.text` when it is `""`.
- **The producer asserts `symbol_sep` occurs in no symbol rendering** before joining. It must never
  appear in graph6/sparse6 printable ASCII, IsalGraph's `NnPpVvCcW`, adjacency `01`, an AGM code or
  a DFS-tuple rendering — but assert it, do not assume it.
- **`length` is always the symbol count**, so `length == len(encoding.split(sep))` when
  `sep != ""` and `length == len(encoding)` when it is `""`. A test asserts both branches.

**Consumer rule (`t06-distance`)**: split on `metadata.symbol_sep` when non-empty and compute the
distance over the resulting **list of symbols**; otherwise over the characters. `rapidfuzz`'s
Levenshtein accepts any sequence of hashables, so this costs nothing. **Never re-encode through the
registry** — that would need the cohort and would break the ownership partition.

> ## ⚠ CORRECTED 2026-08-16 — `encoding` is ALWAYS the symbol join, never `Encoding.text`
>
> Found **independently by both the encoding and the distance tracks**, and verified by the
> orchestrator. `sparse6` and `sparse6_nauty` carry a leading **`':'` format marker in
> `Encoding.text` that is not a symbol**. Measured: `path_graph(4)` → `length = 3`,
> `len(text) = 4`; `cycle_graph(11)` → 11 vs 12; and `len(text) − len(symbols) == 1` on **every** row
> of 50 real graphs for both backends, `0` for graph6 / nauty_graph6 / adjacency / agm_cam /
> isalgraph_pruned. Their symbols *are* single characters, so the original rule assigned
> `symbol_sep = ""` and `encoding = Encoding.text` — under which the invariant this section requires
> a test to assert, `length == len(encoding)`, **fails on row 0**.
>
> **Frozen: `encoding = symbol_sep.join(str(s) for s in Encoding.symbols)` in BOTH branches.** It
> coincides with `Encoding.text` everywhere except sparse6, where it drops the `':'`. `length ==
> len(encoding.split(sep))` then holds universally.
>
> **This is not a new convention — it is the one already frozen.** T-04's article notes §3 record the
> `sparse6` Claim-A column as *"6 bits high on every row — it counted the `':'`, which the frozen
> convention treats as framing, not payload."* Storing the symbol join makes the schema agree with
> the bit convention instead of contradicting it.
>
> **Cost, stated in `metadata.notes` of every affected file**: the stored sparse6 string needs a
> `':'` prepended before `nx.from_sparse6_bytes` will read it. Distance consumers are unaffected —
> they split, never decode — and a constant prefix present in both operands shifts neither
> Levenshtein nor an equal-length test, so no distance changes.

### 3.2 A censored row is NOT an empty row

The original §3 said `encoding` is `''` whenever `status != "ok"`, which **contradicts the D14
invariant** in the same section (`status == "censored"` ⟹ `encoding != ''`). D14 wins: a censored
graph is retained **with its greedy-min fallback string**, which is the whole point of the decision.

| `status` | `encoding` | `fallback_used` | `length` |
|---|---|---|---|
| `ok` | the symbol join | `False` | `>= 0` |
| `censored` | **the greedy-min string** | `True` | `>= 0` |
| `error` | `''` | `False` | `-1` |

`fallback` is retired as a distinct value — it was never distinguishable from `censored`.

### 3.3 The `.npz` string is not self-decodable — a constraint on any future schema change

`decode()` for `graph6`, `sparse6`, `nauty_graph6` and `adjacency` reads `Encoding.wire` (bytes) or
`Encoding.frame`, and §3 carries neither. So round-trip validation is tested as
`backend.decode(backend.encode(G))` on real cohort graphs, with a **separate** test asserting the
`.npz` string recovers the identical symbol sequence. **If a later ticket needs self-decodable
files, `wire` has to enter the schema** — do not assume the stored string suffices.

This keeps `levenshtein` (symbol-level, **primary**) and `levenshtein_char` (character-level,
reported only where explicitly labelled) distinguishable downstream, which is the convention T-04
froze.
| `entropy_bits` | `float64` | `(G,)` | `L·log2(alphabet_size)`; `nan` if `BitCountUndefined` |
| `realised_bits` | `float64` | `(G,)` | format-defined byte length × 8; `nan` if undefined |
| `status` | `<U12` | `(G,)` | `ok` \| `censored` \| `fallback` \| `error` |
| `fallback_used` | `bool` | `(G,)` | `True` iff the D14 greedy-min string was substituted |
| `seconds` | `float32` | `(G,)` | per-graph encode time; `-1` when killed |
| `metadata` | `<U…` | `()` | 0-d JSON, keys in §5 |

**Invariants a test must assert:** `len(graph_ids) == G` for every array; `graph_ids` equals the
cohort file's exactly; `status == "censored"` ⟹ `fallback_used == True` **and** `encoding != ''`
(D14: a censored graph is retained with its greedy-min string, never dropped); `status == "ok"` ⟹
`length >= 0`.

---

## 4. Schema — distance matrix `.npz` (produced by **distance**, consumed by **stats**)

One file per `(suite, dataset, representation, metric)`:
`distances/{suite}/{dataset}__{representation}__{metric}.npz`

**Deliberately mirrors T-05's GED schema so one loader reads both.**

| key | dtype | shape | meaning |
|---|---|---|---|
| `distance_matrix` | `float64` | `(G, G)` | symmetric, zero diagonal, finite, `>= 0` |
| `graph_ids` | `<U16` | `(G,)` | cohort order |
| `node_counts` | `int32` | `(G,)` | carried through, for the size null and the equal-`n` mask |
| `defined_mask` | `bool` | `(G, G)` | `False` where the metric is undefined for that pair (F1) |
| `metadata` | `<U…` | `()` | 0-d JSON, keys in §5 |

**Two derived arrays, same schema, emitted once per dataset (not per representation):**

- `distances/{suite}/{dataset}__size_null.npz` — `distance_matrix[i,j] = abs(n_i - n_j)`. This is
  T-04's trivial baseline and **every printed ρ must carry it**.
- `equal_n_mask` is **not** a file: it is `node_counts[:,None] == node_counts[None,:]`, computed by
  the consumer. Do not materialise it.

**Sharding.** The driver must accept `--chunk-index K --n-chunks N` and emit
`{basename}.shard{K:04d}.npz` holding a contiguous **row band**, plus a merge subcommand producing
the dense file. Model on `benchmarks/real_data/eval_setup/ged_exact_runner.py` and
`ged_merge_shards.py`. Row bands, **not** `itertools.combinations` slices — the consumer indexes by
`(i, j)`.

---

## 5. `metadata` — the same JSON keys in **every** file this wave writes

```json
{
  "schema_version": "t06.1",
  "ticket": "T-06",
  "wave": "2026-08-16-t06-recompute",
  "generated_utc": "<ISO-8601>",
  "seed": 42,
  "suite": "suite1|suite2",
  "dataset": "<key>",
  "representation": "<backend name or null>",
  "metric": "<metric name or null>",
  "n_graphs": 0,
  "isalgraph_engine": "cpp|python",
  "isalgraph_build_hash": "<isalgraph.build_info()['build_hash']>",
  "code_commit": "<git rev-parse HEAD of the checkout that PRODUCED this>",
  "src_commit": "<git -C /home/mpascual/research/code/IsalGraph rev-parse HEAD>",
  "encode_budget_s": 300.0,
  "notes": ""
}
```

**`isalgraph_build_hash` and `src_commit` are not optional.** They are the only way to detect
afterwards that a run picked up another branch's `src/`. A file without them is unusable evidence.

---

## 6. Environment — verbatim

```bash
PY=/home/mpascual/.conda/envs/isalgraph-cpp/bin/python
cd <YOUR WORKTREE>                       # never the shared checkout

$PY -m pytest tests/unit/test_t06_<track>.py -q     # your own tests, fast
$PY -m pytest tests/unit/ -q                        # unit suite, before you finish
$PY -m ruff check --fix src/ tests/ benchmarks/
$PY -m mypy src/isalgraph/
```

Assert `isalgraph.engine() == "cpp"` in any script whose behaviour depends on the engine.
Expected `build_hash` today: `298fc1188bf1b051`.

**Style** (`CLAUDE.md`): Python 3.10+ syntax (`X | None`, `list[int]`), **full type annotations on
every signature**, Google-style docstrings, `logging` not `print` in library code, dataclasses for
anything crossing a function boundary, no nesting beyond 2 levels. `ruff` and `mypy --strict` must be
clean. Conventional commits, no `Co-authored-by`.

---

## 7. Definition of done, for every track

1. Your module runs end to end on a **small real** input (a ≤ 200-graph slice of a real dataset), not
   only on synthetic data.
2. Your tests pass, and the **whole unit suite** still passes.
3. `ruff` clean, `mypy --strict` clean.
4. **Everything committed.** `git status --porcelain` empty. Uncommitted work does not exist as far
   as a merge is concerned, and sessions die.
5. **Commit incrementally**, not once at the end.
6. Your work log exists at `.claude/notes/2026-08-16-t06-recompute/<track>.md` **inside your
   worktree**, committed, following §8.

---

## 8. Work log — required sections

```markdown
# <track> — work log
**Branch** `t06/<track>` · **Base** `8afa59e` · **Head** `<sha>`

## What I built
One paragraph, then a table: file -> purpose -> lines added.

## Acceptance criteria
One row per criterion from your prompt: criterion | command run | result (verbatim).

## Decisions I made, and why
Anything the prompt did not specify. Name the alternative you rejected.

## Assumptions I recorded rather than blocking on
Each with the message id if you told the orchestrator.

## What I could NOT do, and why
Be explicit. An honest gap is worth more than a claim I have to discover is false.

## Contract defects found
Anything in CONTRACTS.md that was wrong, ambiguous, or impossible. This section
being non-empty is a SUCCESS, not a failure.
```

---

## 9. Peers

| Name | Mission | Ownership |
|---|---|---|
| `t06-encoding` | Encoding campaign + Claim A bits + D14 censoring + the `s`-term completion rates | `benchmarks/real_data/eval_encoding/t06_*.py`, `tests/unit/test_t06_encoding.py` |
| `t06-distance` | Sharded distance-matrix driver + size null + defined-mask | `benchmarks/real_data/eval_distance/**`, `tests/unit/test_t06_distance.py` |
| `t06-stats` | D2/D3/D4/D7/D8/D9/D15 + the F0/F1/F2 family runner + `N_actual` | `benchmarks/real_data/eval_stats/**`, `tests/unit/test_t06_stats.py` |

**`main` is the orchestrator.** Message it, not each other, for anything touching a contract.

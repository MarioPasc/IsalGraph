# T-04a track B — F5 (metric feasibility, descriptive arm)

Branch `ticket/T-04a`, base commit `7e96f4a`. Commits: `685ee29` (F5, bootstrap, bounds loader),
`8d0f0ba` (log), `934a8f1` (§6.1 fix), `66d7dbe` (§6.2 fix), `3d95e80` (§6 and §7), plus this
correction.

> **§6 records two defects the demonstration runs did not catch and the full protocol did.** Both
> were mine, both were contract violations, and both were invisible to a 40-graph run against a
> hand-written fixture grid. The lesson is in §6.3.

---

## 1. What I changed, file by file

### `src/isalgraph/competitors/bootstrap.py` (new)

Graph-level percentile bootstrap. Three decisions worth naming:

- **`make_resample_index(n_graphs, *, resamples, seed)` is separate from
  `graph_bootstrap_ci(x, y, pair_index, index, *, alpha, min_pairs)`.** The brief allowed an
  "equivalent signature you document": this is it. Splitting them is what makes D7 enforceable —
  the caller builds one `ResampleIndex` per (dataset, view) and hands the *same object* to every
  representation, so their intervals are paired rather than independently noisy. A test asserts
  the object identity is shared, by spying on `f5.graph_bootstrap_ci`.
- **`random.Random(seed)`, not `numpy.random.default_rng`.** CONTRACTS §5 names `random.Random(42)`
  explicitly; the brief permitted either. I took the contract's, so there is no discrepancy to
  reconcile later. Determinism is asserted.
- **Self-pairs are dropped by construction.** A replicate draws graphs with replacement, so one
  graph can occupy two slots. Pairing it with itself would add a distance-0/GED-0 point that was
  never observed, biasing rho upward exactly where the distribution is tightest. The pair lookup
  table's diagonal is never written, so a duplicated slot resolves to "absent" rather than to a
  fabricated zero. Tested with `n_graphs=2`.
- `None` is returned rather than a fabricated interval when fewer than two replicates produce a
  defined rho (each replicate needs ≥ 3 surviving pairs).
- Inner rho is `rankdata` + Pearson rather than `scipy.stats.spearmanr`: the p-value scipy computes
  on every replicate is never read and dominated the cost. Same statistic. The **point estimate**
  still goes through `spearmanr`, so the reported `p` is scipy's.

### `src/isalgraph/competitors/ged_reference.py` (additive only)

`load_ged` is untouched — `reproduce.py` keeps working unchanged. Added `GEDBounds`,
`load_bounds(dataset, which)`, `BOUNDS_SUBDIR/BOUNDS_DIR/BOUNDS_KEY`, `MAX_OFFDIAG_ZERO_FRACTION`.

- `graph_ids` alignment against the cohort is **asserted**, as `load_ged` does.
- `GEDBounds.finite_pairs` filters with `np.isfinite`. A GED of exactly 0 is kept.
- Matrix-level guards at load: any negative finite entry aborts; an off-diagonal exact-zero fraction
  ≥ 0.99 aborts. That is the shape of GEDLIB's silent wrong-accessor failure. There is **no**
  per-pair `value > 0` rule.
- `load_bounds(ds, "mid")` raises. There is no third side of the bracket.

### `src/isalgraph/competitors/f5.py` (rewritten)

New CLI: `--grid --out --resamples --seed`, **plus `--n-graphs` and `--datasets`**. `--n-graphs` is
required by acceptance criterion 5 (a 40-graph demonstration run); its default is the frozen 200 and
`--datasets` defaults to every cohort present under the root. The old `--metric` flag is gone.

`grep -rn "competitors.f5\|competitors import f5" src/ tests/ benchmarks/ experiments/` finds **no
programmatic caller** — only prose in `__init__.py`/`grid.py`/`README.md` and
`tests/unit/test_competitors_core.py::test_f5_is_the_only_entry_point_that_can_see_ged`, which
asserts the import closure and still passes. Nothing broke.

Each representation runs under its own `primary_distance` from the grid. Both views, the size null in
every view, `n_unencodable` reported per backend, `::lb` and `::ub` as two records that are never
averaged. The module docstring still states F5 is descriptive and is not an input to selection.

### `tests/unit/test_competitors_f5.py` (new) — 24 tests

Fully synthetic: the fixture writes a six-graph CSR cohort, an exact-GED file and both bound files
under a temporary `ISALGRAPH_COHORT_ROOT`, then clears the `functools.cache`s. One test is marked
`integration` and skips when the external drive is unmounted.

---

## 2. The two demonstration runs

Fixture grid: `scratchpad/grid_fixture.json` (hand-written per the brief; track A's real grid had
not landed). It sets `min_dfs` to `null` deliberately so the printed-absence path is exercised, and
omits nothing else.

### Run 1 — Suite 1, `iam_letter_low`, 40 graphs, 200 resamples

```
python -m isalgraph.competitors.f5 --grid grid_fixture.json --out f5_demo1.json \
  --datasets iam_letter_low --n-graphs 40 --resamples 200
```

**Wall time 0.82 s** (`real 0m0.907s`). Record `iam_letter_low`, `reference: "exact"`.

| representation | all_pairs rho | vs null | 95 % CI | n | equal_n rho | equal_n CI |
|---|---|---|---|---|---|---|
| **size_null** | **0.9155** | — | — | 780 | *undefined* | — |
| adjacency | 0.6932 | −0.2223 | [0.4580, 0.9119] | 178 | 0.6932 | [0.4580, 0.9119] |
| agm_cam | 0.9283 | +0.0128 | [0.8885, 0.9508] | 780 | 0.9972 | [0.9854, 1.0000] |
| graph6 | 0.7294 | −0.1861 | [0.5995, 0.8696] | 780 | 0.7067 | [0.5019, 0.9097] |
| **isalgraph_canonical** | **0.9232** | **+0.0077** | [0.8823, 0.9589] | 780 | **1.0000** | [1.0000, 1.0000] |
| isalgraph_pruned | 0.9100 | −0.0055 | [0.8641, 0.9500] | 780 | 0.9846 | [0.9533, 0.9993] |
| min_dfs | *null* | — | — | 0 | *null* | — |
| nauty_graph6 | 0.7153 | −0.2001 | [0.5861, 0.8562] | 780 | 0.9801 | [0.9416, 0.9983] |
| sparse6 | 0.7731 | −0.1423 | [0.6512, 0.8911] | 780 | 0.7249 | [0.5214, 0.9102] |
| sparse6_nauty | 0.6678 | −0.2477 | [0.4938, 0.8273] | 780 | 0.9825 | [0.9513, 1.0000] |
| wl_subtree | 0.7417 | −0.1738 | [0.6098, 0.8648] | 780 | 0.9818 | [0.9488, 0.9983] |

`adjacency`'s n = 178 is not a defect: `hamming` requires equal-length symbol sequences, so it is
defined only on equal-`n` pairs. `n_undefined = 602` reports it, and its two views coincide.

**The bootstrap earns its place immediately.** IsalGraph's +0.0077 over the size null on `all_pairs`
sits inside an interval of half-width ~0.04. At 40 graphs that gap is not distinguishable from zero,
and a bare "0.9232 vs 0.9155" would have read as a result.

### Run 1b — the same dataset at the **full frozen protocol** (for sizing)

```
python -m isalgraph.competitors.f5 --grid grid_fixture.json --out f5_full_letterlow.json \
  --datasets iam_letter_low          # 200 graphs, 2000 resamples
```

**Wall time 29.7 s** for one Suite-1 dataset. all_pairs: null 0.8991; `isalgraph_canonical` 0.9351
[0.9192, 0.9489] (n = 19,900), `isalgraph_pruned` 0.9253, `agm_cam` 0.9176, `adjacency` 0.5786
(n = 4,552). At 200 graphs the +0.0360 gap over the null *is* outside the interval; at 40 it was not.

Sizing for the orchestrator's run: 29.7 s here with all 19,900 pairs certified. Suite 2 costs about
twice that per dataset (two references) plus encoding of larger graphs. **Ten datasets should land
in well under an hour on this workstation**, single-threaded.

### Run 2 — Suite 2, `grec`, 40 graphs, 200 resamples

```
python -m isalgraph.competitors.f5 --grid grid_fixture.json --out f5_demo2.json \
  --datasets grec --n-graphs 40 --resamples 200
```

**Wall time 1.09 s.** Two records present and distinct: `grec::lb` (BRANCH_FAST) and `grec::ub`
(BIPARTITE). No `::mid` key exists and none can be produced.

all_pairs, both references (`n = 780` unless noted):

| representation | `::lb` rho | `::ub` rho |
|---|---|---|
| **size_null** | **0.9792**  [0.9621, 0.9906] | **0.7436**  [0.5762, 0.8426] |
| adjacency (n = 64) | 0.4966 | 0.8124 |
| graph6 | 0.8214  [0.6665, 0.9044] | 0.9280  [0.8233, 0.9566] |
| **isalgraph_pruned** | **0.8008**  [0.6442, 0.8761] | **0.8204**  [0.6708, 0.8977] |
| nauty_graph6 | 0.8497  [0.7315, 0.9223] | 0.9107  [0.7870, 0.9517] |
| sparse6 | 0.7610 | 0.9066 |
| sparse6_nauty | 0.7674 | 0.8987 |
| wl_subtree | 0.8455  [0.6921, 0.9302] | 0.7500  [0.5290, 0.8476] |
| agm_cam, isalgraph_canonical | *refused* (§3.1) | *refused* (§3.1) |

**The open question, answered on one dataset — and the answer flips with the side of the bracket.**

- Against the **upper** bound the size null scores 0.7436 and `isalgraph_pruned` clears it
  (0.8204, +0.0768), though `graph6` (0.9280) and `nauty_graph6` (0.9107) clear it by more.
- Against the **lower** bound the size null scores **0.9792 — higher than every representation
  measured**, `nauty_graph6`'s 0.8497 included. Nothing clears it, and the intervals do not
  overlap the null's.

That is not noise, and it is worth stating in the paper rather than burying: BRANCH_FAST's lower
bound on GREC is close to a monotone function of the node-count difference, so a rho against it
mostly measures how well a representation tracks graph *size*. **The `lb` arm therefore cannot
support a claim about structure**, and reporting a midpoint of the two would have averaged a
size-dominated number with a structure-sensitive one into a quantity with no interpretation. This
is the clearest justification I have seen for the no-interpolation rule, and it is an argument for
leading the Suite-2 discussion with `ub`.

The **`equal_n` view removes the size channel and reverses the ordering again**: on `lb`,
`wl_subtree` 0.9734 [0.8794, 0.9978], `nauty_graph6` 0.9494, `isalgraph_pruned` 0.9279 [0.7249,
0.9902], `graph6` collapses to 0.5365 [−0.0655, 0.7910]. At n = 64 pairs those intervals are wide
enough that the ordering among the top three is not resolved — which is exactly the statement the
bootstrap exists to let us make.

---

## 3. Things in the brief that turned out to be wrong or incomplete

Reporting these plainly, as instructed. Items 1 and 2 changed the code's behaviour.

### 3.1 The Suite-2 arm **cannot** report `isalgraph_canonical` — the question as posed is unanswerable

The brief's stated open question is "whether IsalGraph clears the size null anywhere in Suite 2".
`isalgraph_canonical` declares `Capability.SUITE1_ONLY` and raises `SuiteScopeError` above n = 12.
Measured on the 40-graph GREC draw: **11 of 40 graphs refused, 406 of 780 pairs surviving** — and
the 374 lost pairs are precisely the hardest ones. `base.table_scope_error` is the frozen rule for
this (design criteria 5 and 9): a backend may be *run* on Suite 2 to measure its ceiling, but it may
not contribute a *printed row* there, because the row would be conditioned on the graphs that
happened to finish.

I applied that rule. `agm_cam` and `isalgraph_canonical` are emitted on every Suite-2 record as a
printed absence carrying `table_scope_error`'s reason, and are not encoded at all there.
**IsalGraph is represented in Suite 2 by `isalgraph_pruned` only**, which is not `SUITE1_ONLY` and
encoded all 40 GREC graphs. If the intent was to report the canonical variant's Suite-2 rho anyway,
that is a decision to overrule a frozen criterion and it is yours, not mine — tell me and I will
add it behind an explicit flag.

### 3.2 The size null is **undefined by construction** in the equal-`n` view, and NaN is not JSON

`rho(|n₁ − n₂|, GED)` on a view where `n₁ == n₂` correlates a constant column. `scipy` returns
`NaN` and warns; it does not raise. `json.dump` then writes the bare token `NaN`, which is not valid
JSON (RFC 8259) and which a strict downstream parser reads as either a failure far from the cause or
as a number.

Fixed two ways: any constant column now produces a printed absence with a stated reason
(`"Spearman undefined: the 'size_null' distance is constant over the pairs of this view..."`), and
the writer passes `allow_nan=False` so anything I missed aborts at the write rather than downstream.

**Track C must handle `size_null.rho is None` in the `equal_n` view.** It happens on every dataset,
in every run. This is not an edge case; it is the definition of the view.

### 3.3 CONTRACTS §5 — three additions, all supersets, none renaming a frozen field

Flagging rather than assuming. Nothing in §5 was removed or renamed.

1. **Every cell carries the same eight keys**, `size_null` included:
   `metric, rho, p, ci, n_pairs, n_undefined, zero_frac, reason`. §5 shows `size_null` with four.
   The superset lets track C index one shape.
2. **`n_unencodable[<backend>]` can be `null`**, not only an int — for a representation that was
   never attempted (no admissible distance, or refused by §3.1). `0` there would assert that every
   graph encoded, which is false.
3. **Extra keys**: `wall_seconds` (top level), `n_reference_pairs` and `encode_errors` (per record).
   `encode_errors` maps backend → `{ExceptionName: count}`.

Also added, and not in §5: a representation present in `available_backends()` but **absent** from
the grid's `primary_distance` map is emitted with `reason = "absent from the grid's
primary_distance map"` rather than dropped, so a silently shrinking grid cannot silently shrink the
F5 table. It is told apart from `"no admissible distance (T-04a selection)"`.

### 3.4 The censoring trap is real but currently inert on these five cohorts

The brief warns that censored values are `inf`. Verified across all five Suite-2 datasets
(`grec`, `aids_iam`, `coil_del`, `mutagenicity`, `protein`), both `LB` and `UB`:
**zero non-finite entries and zero negative entries**. `grec`'s metadata reports
`censoring_rate: 0.0`. The `np.isfinite` filter is kept regardless — it costs nothing and the
`UB_TIGHT`/`UB_SENSITIVITY` trees are not guaranteed to share that property.

### 3.5 `graph_ids` alignment — verified on all five, not just `mutagenicity`

The brief verified `mutagenicity` (4,040). I checked all five Suite-2 cohorts against **both** the
`LB` and the `UB` file: `grec` 650, `aids_iam` 1,811, `coil_del` 3,900, `mutagenicity` 4,040,
`protein` 569 — exact match in every case. Asserted in code anyway.

### 3.6 `LB/<ds>.npz` and `UB/<ds>.npz` each carry **both** `lb_matrix` and `ub_matrix`

Reading the wrong key from the right file is therefore silent. The loader hard-codes
`BOUNDS_DIR[which]` and `BOUNDS_KEY[which]` together so the pair cannot come apart, and a test
asserts `lb.values != ub.values` and `lb <= ub` elementwise.

### 3.7 `aids_graphedx` — confirmed not needed

It exists under `APPROX_GED/{LB,UB}/` but is not in `datasets.ALL_DATASETS`, so `load_bounds` on it
raises `DatasetNotFoundError` through `suite_of` before any positional join can happen. Suite-1
`aids` (769) has **no** LB/UB file at all, which is consistent: Suite 1 uses certified exact GED.

---

## 4. Acceptance criteria

`PY=~/.conda/envs/isalgraph-cpp/bin/python`; `$PY -c "import isalgraph; print(isalgraph.engine())"`
prints `cpp`. `PYTHONPATH` was never set.

**1. `$PY -m pytest tests/unit/test_competitors_f5.py -q`**

```
24 passed in 0.59s
```

**2. `$PY -m pytest tests/unit/test_competitors_core.py -q`**

```
2 failed, 37 passed in 0.27s
```

The three import-closure tests — the ones this track must keep green — pass:

```
$PY -m pytest tests/unit/test_competitors_core.py -q -k "closure or ged"
3 passed, 36 deselected in 0.13s
```

The two failures are **pre-existing and not mine**:
`test_the_grid_refuses_a_suite1_only_row_on_a_suite2_dataset` fails with
`TypeError: measure_cell() missing 1 required positional argument: 'f3cache'`, and
`test_grid_refuses_to_select_a_baseline_as_a_primary_distance` fails on
`size_null.passes_selection == True`. Both are `grid.py` behaviour; `git status` shows `grid.py`
unmodified since `7e96f4a`, so both fail at the base commit. They are CONTRACTS §4's candidate rule,
which is **track A's** work item.

**3. `$PY -m ruff check src/isalgraph/competitors/ tests/unit/test_competitors_f5.py`**

```
All checks passed!
```

**4. `$PY -m mypy src/isalgraph/competitors/f5.py src/isalgraph/competitors/ged_reference.py src/isalgraph/competitors/bootstrap.py`**

```
Success: no issues found in 3 source files
```

**5. Suite-1 demonstration run** — §2 run 1. Wall time **0.82 s**. JSON validated field by field
against CONTRACTS §5 (top-level key set, per-record key set, per-cell key set) by a checker script;
all present, with the three documented supersets of §3.3.

**6. Suite-2 demonstration run** — §2 run 2. Wall time **1.09 s**. `grec::lb` and `grec::ub` both
present, both `reference` fields correct, rho values distinct on every representation
(e.g. `graph6` 0.8214 vs 0.9280; the size null 0.9792 vs 0.7436).

---

## 5. For the orchestrator — decisions I need from you

1. **§3.1** — do you accept that `isalgraph_canonical` gets no printed Suite-2 F5 row, or do you
   want the ceiling number behind an explicit flag? This determines what the Suite-2 arm can claim.
2. **§3.2** — track C must handle `size_null.rho is None` in `equal_n`. Please relay.
3. **§3.3** — three CONTRACTS §5 supersets to confirm. `n_unencodable` admitting `null` is the one
   most likely to surprise track C.
4. **§2 run 2** — on GREC the size null beats every representation against the **lower** bound
   (0.9792) and loses to most of them against the **upper** bound (0.7436). If that holds across
   the other four Suite-2 cohorts, the paper should lead the Suite-2 discussion with `ub` and say
   plainly why. I have not checked the other four; that is your full run.
5. `src/isalgraph/competitors/README.md` line 82 still describes the old
   `python -m isalgraph.competitors.f5` invocation. I do not own it; the CLI is now
   `--grid <grid.json> --out <path> [--resamples N] [--seed S] [--n-graphs K] [--datasets a,b]`.

---

## 6. Two defects the demonstration runs missed and the full protocol found

### 6.1 `encode_draw` caught `CompetitorError`, which is the wrong branch — `934a8f1`

The full protocol crashed and wrote nothing:

```
f5.py:146   encoded[idx] = backend.encode(graph)
  -> backends/isalgraph_ref.py:155 -> core/backends.py:358
  -> isalgraph.errors.CanonicalizationTimeoutError
```

The hierarchy has two branches under one root, and I guarded only one:

```
IsalGraphError
├── CompetitorError      AGMBudgetExceeded, MinDfsBudgetExceeded, SuiteScopeError, BudgetExceeded
└── EncodingError
    └── CanonicalizationTimeoutError      <- the core engine's wall clock, NOT a CompetitorError
```

`isalgraph_ref` lets the core engine's timeout propagate unchanged, so my guard caught every
*competitor backend's own* cap and missed the *engine's*. CONTRACTS §5 and my brief both required
an unencodable graph to be counted and excluded; `grid.py` had already hit the identical situation
and caught broadly. **The catch is now `IsalGraphError`** — the common base, still narrow enough
that a `TypeError` in our own code propagates. Kinds stay counted under their own names, never
summed, for T-06's `error_kind` column.

I also found and guarded a second instance of the same shape: `VectorBackend.fit()` is called
**outside** the per-graph loop and so was outside the per-graph guard. A raise there would have
taken the run down for one representation. It is now a reported total failure for that backend.

Two regression tests, both **verified to fail against the old catch** before being kept.

### 6.2 The grid's `size_null: null` entry deleted the null row — `66d7dbe`

Found while inspecting track A's real `grid_200.json` before re-running. It lists

```json
"size_null": null
```

correctly — CONTRACTS §4 forbids selecting a baseline as any representation's primary distance. But
`size_null` is *also* the dedicated baseline row every other row is judged against. My
`dataset_record` wrote the null row, then fell through to the representation loop, hit `size_null`
with a `None` distance, and **overwrote the null row with a printed absence**. The entire F5 table
would have shipped with no size null in any view of any dataset — the one column the whole arm
exists to print — and nothing would have raised, because a printed absence is a legitimate value
everywhere else.

The baseline is now emitted once, before the loop, and the loop skips it. Test added on a grid
shaped like the real one.

### 6.3 What this says about the verification, plainly

Both defects were reachable only through inputs my demonstration runs did not have:

- **§6.1 needed large graphs.** The canonicalisation budget is 2.0 s; 40 Letter or GREC graphs never
  approach it. A demo on small graphs is not coverage of the failure path — it is coverage of the
  path where the failure does not occur.
- **§6.2 needed the real grid.** I hand-wrote the fixture grid per the brief and omitted a key I did
  not know would be there. A fixture written by the consumer cannot test the producer's schema.

Both are now covered by unit tests that construct the triggering input directly, which is the only
form of coverage that does not depend on a cohort being mounted or a graph being large enough.

---

## 7. The full protocol run

```bash
$PY -m isalgraph.competitors.f5 \
  --grid /media/mpascual/Sandisk2TB/research/isalgraph/T-04a/grid_200.json \
  --seed 42 --n-graphs 200 --resamples 2000 \
  --out /media/mpascual/Sandisk2TB/research/isalgraph/T-04a/f5_200.json
```

**Completed. Wall time 715.3 s (11.9 min), single-threaded.** 15 records (5 Suite-1 + 5 Suite-2 x 2
bounds), 194 KB. `linux` draws 89 graphs, not 200 — its cohort holds only 89.

My earlier "under an hour" estimate was extrapolated from `iam_letter_low` and was loose in the
right direction; 11.9 min is the real number. The first complete run took 635.9 s; the §9 fix adds
79 s, all of it the second bootstrap on the four censored cells.

### 7.1 `n_unencodable` with the error-kind breakdown, and the budget each backend ran under

Budgets are the frozen defaults; no `Budget` object was passed, so every backend used its own.

| backend | budget | value |
|---|---|---|
| `isalgraph_canonical`, `isalgraph_pruned` | `Budget.timeout_s` (wall clock) | **2.0 s** (`isalgraph_ref.DEFAULT_TIMEOUT_S`) |
| `agm_cam` | `Budget.search_nodes` (branch-and-bound nodes) | **200,000** (`agm.DEFAULT_NODE_BUDGET = SUITE1_NODE_BUDGET`) |
| `min_dfs` | `Budget.max_projections` (live embeddings, a MEMORY cap) | **50,000** (`min_dfs.MAX_PROJECTIONS`) |
| `nauty_graph6`, `sparse6_nauty`, `wl_subtree` | none declared | — |

Every dataset not listed had **zero** unencodable graphs for every attempted backend.

| dataset | suite | n | ref pairs | backend | n_unencodable | error kind | budget that ran out |
|---|---|---|---|---|---|---|---|
| `aids` | suite1 | 200 | 15,634 | `agm_cam` | **2** | `AGMBudgetExceeded` | `search_nodes = 200,000` |
| `mutagenicity` | suite2 | 200 | 19,900 | `isalgraph_pruned` | **14** | `CanonicalizationTimeoutError` | `timeout_s = 2.0` |
| `mutagenicity` | suite2 | 200 | 19,900 | `min_dfs` | **14** | `MinDfsBudgetExceeded` | `max_projections = 50,000` |

The kinds are distinct columns and are never summed: `AGMBudgetExceeded` and `MinDfsBudgetExceeded`
are **scope decisions** (an internal cap we chose), `CanonicalizationTimeoutError` is a **budget
outcome** (a wall clock we set). `mutagenicity` is the only dataset where the failure you predicted
fires, at 14/200 rather than 9/200 — the difference is the draw, since this is a per-dataset
`Cohort.sample(200, seed=42)` and not the pooled 200.

`agm_cam` and `isalgraph_canonical` carry `n_unencodable = null` on all ten Suite-2 records: both
are `SUITE1_ONLY` and were never attempted there (§3.1).

### 7.2 The scientific result, stated plainly

**The verdict is a confidence interval on the *paired difference*, not a comparison of two marginal
intervals.** See §8 — I got this wrong in my first report and it understated our own method. The
authority for every interval quoted here is
`/media/mpascual/Sandisk2TB/research/isalgraph/T-04a/paired_null_ci.json`: paired graph-level
bootstrap, 2,000 resamples, seed 42, both arms on the identical pair set, and for `mutagenicity`
that pair set is IsalGraph's own 17,205 (§9), not the cohort's 19,900.

**IsalGraph clears the size null on 6 of the 15 records, and every one of the 15 differences
excludes zero.** There is no undecided record in this table.

| record | IsalGraph rho | null (on the shared pairs) | paired difference | 95 % CI | verdict |
|---|---:|---:|---:|---|---|
| `iam_letter_low` | 0.925 | 0.899 | **+0.026** | [+0.008, +0.046] | **clears, significant** |
| `iam_letter_med` | 0.875 | 0.919 | −0.044 | [−0.068, −0.014] | below, significant |
| `iam_letter_high` | 0.697 | 0.917 | −0.220 | [−0.260, −0.183] | below, significant |
| `linux` | 0.474 | 0.713 | −0.239 | [−0.320, −0.157] | below, significant |
| `aids` | 0.256 | 0.784 | −0.528 | [−0.571, −0.482] | below, significant |
| `aids_iam::ub` | 0.739 | 0.712 | **+0.027** | [+0.002, +0.049] | **clears, significant** |
| `grec::ub` | 0.828 | 0.706 | **+0.122** | [+0.093, +0.151] | **clears, significant** |
| `coil_del::ub` | 0.933 | 0.736 | **+0.197** | [+0.164, +0.235] | **clears, significant** |
| `mutagenicity::ub` | 0.832 | **0.636** | **+0.196** | [+0.162, +0.226] | **clears, significant** |
| `protein::ub` | 0.843 | 0.460 | **+0.383** | [+0.326, +0.443] | **clears, significant** |
| `aids_iam::lb` | 0.712 | 0.960 | −0.248 | [−0.320, −0.194] | below, significant |
| `grec::lb` | 0.766 | 0.980 | −0.214 | [−0.254, −0.182] | below, significant |
| `coil_del::lb` | 0.916 | 0.998 | −0.082 | [−0.096, −0.069] | below, significant |
| `mutagenicity::lb` | 0.693 | **0.982** | −0.289 | [−0.333, −0.246] | below, significant |
| `protein::lb` | 0.737 | 0.970 | −0.233 | [−0.276, −0.197] | below, significant |

Three findings. None of them flatters us, and all of them are F5 — descriptive, and no input to
selection.

1. **On Suite 1, IsalGraph clears the size null on 1 of 5, significantly.** The `iam_letter_low`
   margin is +0.026 with CI [+0.008, +0.046], which excludes zero. It is a small effect and it is a
   real one. On the other four IsalGraph is below the null, all four significantly. **A reviewer who
   computes the size null on Suite 1 still has a real objection on four of five datasets**, and the
   honest answer is the `equal_n` view plus Claim A, not rho on all pairs.
2. **The `lb` arm is a size proxy across the whole cohort, not just GREC.** The size null scores
   0.960–0.998 against BRANCH_FAST on all five Suite-2 datasets and beats every representation on
   every one, with all five differences excluding zero. BRANCH_FAST's bound is close to a monotone
   function of the node-count difference. **The `lb` arm cannot support any claim about structure**,
   and averaging it with `ub` would have produced a midpoint with no interpretation. This is the
   measured justification for the no-interpolation rule.
3. **Against `ub`, IsalGraph clears the null on all five Suite-2 datasets — and is still the best
   representation on none of the 15 records.** Counting the top representation per all-pairs record:
   `min_dfs` 4, `agm_cam` 3, `wl_subtree` 3, `sparse6_nauty` 3, `nauty_graph6` 2, **IsalGraph 0**.
   `min_dfs` beats IsalGraph on four of five Suite-1 datasets. Clearing the null and being the best
   representation are different claims, and only the first one holds.

The `equal_n` view removes the size channel and the picture improves markedly for IsalGraph on the
Letter family (`low` 0.981, `med` 0.911) while staying weak on `linux` (0.397) and `aids` (0.219).
The size null is a printed absence in every `equal_n` view, by construction (§3.2).

---

## 8. A statistical error in my §7.2, and a second one inside it

I wrote that Letter LOW's margin "has overlapping intervals, so it is not a separation", and marked
four records "clears, CIs overlap" as though that qualified the finding.

**That inference is invalid.** Two marginal confidence intervals can overlap substantially while the
confidence interval on their *difference* still excludes zero. The overlap rule is conservative and
its conservatism is not small — Schenker and Gentleman, *The American Statistician* 55(3):182–186
(2001), "On Judging the Significance of Differences by Examining the Overlap Between Confidence
Intervals". The correct test is an interval on the paired difference, which is what
`paired_null_ci.json` computes: both arms on the identical pair set, on shared resamples, so the
resampling noise common to the two cancels instead of adding.

Measured, the two records I had written off:

| record | paired difference | 95 % CI | marginal intervals |
|---|---:|---|---|
| `iam_letter_low` | **+0.026** | **[+0.008, +0.046]**, excludes 0 | overlap heavily |
| `aids_iam::ub` | **+0.027** | **[+0.002, +0.049]**, excludes 0 | overlap heavily |

Both are significant. **IsalGraph does clear the size null on Letter LOW, and on 1 of 5 Suite-1
datasets rather than on none.**

**A second error inside the same section, which I found while correcting the first.** I wrote
"clears on 5 of the 15 all-pairs records" while my own table listed six. The count is **6 of 15**:
`iam_letter_low` plus all five `::ub` records. A miscount in the headline of the section whose
whole job is to state the count.

Both errors ran in the same direction — against our own method. That direction is the safer one to
err in and it is still an error: a conservative mistake in a revision is a claim we could have made
and did not, and the reviewer does not credit us for the caution. The rule for the rest of this
ticket: **a difference between two quantities is tested with an interval on the difference, never by
eyeballing two marginal intervals**, and any count in a headline is recomputed from the table
underneath it rather than typed.

## 9. The size null was computed on the wrong pair set — `1651d6b`

Reported by the orchestrator, verified independently before implementing.

`rho - size_null` subtracted a null computed over the cohort's **whole** pair set from a rho computed
over the representation's **own** pair set. Wherever a representation lost graphs, those are two
correlations over two different samples and the difference is not a comparison.

**Verified on `mutagenicity`, the only dataset where a selected representation loses graphs:**

| quantity | value |
|---|---|
| `isalgraph_pruned` rho, `::ub` | 0.8322 (unchanged — it was always on its own subset) |
| null over all 19,900 pairs | 0.7538 |
| null over IsalGraph's 17,205 pairs | **0.6363** |
| margin | +0.078 → **+0.196** |

**The censoring is maximally size-biased, which is why it moves the null and not the arm.** The 14
censored graphs are the ones that exhausted the 2.0 s canonicalisation budget. Measured on the draw:
censored graphs average **75.8 nodes (max 97)**, kept graphs **25.4 (max 48)** — *every censored
graph is larger than every kept one*. Removing them collapses the spread of `|n1 - n2|` from
sd 16.4 to sd 8.0, and a null that is a function of `|n1 - n2|` loses exactly that much signal. The
representation's own rho does not move at all. This is D14's censoring bias appearing **inside the
baseline** rather than inside the arm — the direction nobody checks — and it lands on Mutagenicity,
the one dataset where IsalGraph wins Claim A outright.

**The restriction has to be per representation, not per dataset.** `min_dfs` is also censored 14/200
on Mutagenicity, but by `MinDfsBudgetExceeded` on a *different* 14 graphs, and its restricted null
lands on **0.6817**, not IsalGraph's 0.6363. One null per dataset would have been wrong for at least
one of them.

**What changed.** Every cell now carries `size_null_on_my_pairs` — rho, p, CI and n_pairs on exactly
that cell's pairs, computed on the same shared resample indices. The row-level `size_null` stays: it
is the right thing to print in a standalone null column, and a reader needs to see the two differ
and by how much, so the printed summary appends `[null 0.7538->0.6363 on its pairs]` whenever they
diverge by more than 5e-4. A cell that kept every pair of its view reuses the row null rather than
paying for a second bootstrap, so the cost falls only on the censored cells.

**This is a CONTRACTS §5 change and track C must be told**: `size_null_on_my_pairs` is a new key in
every cell (`null` in a printed absence), and **it, not the row-level `size_null`, is the valid
subtrahend for any per-representation difference**. On the 14 uncensored records the two agree to
within 1e-9, so a reader who uses the wrong one sees nothing wrong until Mutagenicity.

### 9.1 Cross-check against the authority file — 15 of 15, zero mismatches

`size_null_on_my_pairs` is compared against `rho_null` in `paired_null_ci.json` on every record.
**All 15 agree to better than 1e-6**, including the two where the restricted null departs from the
row-level one:

| record | row-level `size_null` | `size_null_on_my_pairs` | authority `rho_null` |
|---|---:|---:|---:|
| `mutagenicity::lb` | 0.9876 | **0.9816** | 0.9816 |
| `mutagenicity::ub` | 0.7538 | **0.6363** | 0.6363 |
| the other 13 | — | identical to the row null | identical |

The two implementations were written independently — mine inside `rho_record`, the orchestrator's as
a standalone paired bootstrap — so this is a genuine cross-check rather than a tautology. It also
confirms the reuse shortcut is sound: the 13 records where a cell kept every pair return the row
null unchanged, and both differing records are on the one dataset with censoring.

Censoring in the final run, unchanged from §7.1: `aids` `agm_cam` 2/200 (`AGMBudgetExceeded`,
`search_nodes = 200,000`); `mutagenicity` `isalgraph_pruned` 14/200
(`CanonicalizationTimeoutError`, `timeout_s = 2.0`) and `min_dfs` 14/200 (`MinDfsBudgetExceeded`,
`max_projections = 50,000`). Every other dataset loses nothing.
